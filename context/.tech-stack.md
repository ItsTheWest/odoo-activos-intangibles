# Tech Stack

This document specifies the technical stack and dependencies for the `gestion_activos_intangibles` module.

## 1. Core Framework
- **Odoo Version**: [Specify Version, e.g., Odoo 17]
- **Python Version**: [Specify Version, e.g., Python 3.10+]
- **Database**: PostgreSQL

## 2. Dependencies
- **Odoo Base Modules**: `base`, `account` (if financial tracking is needed), `mail` (for chatter).
- **External Python Libraries**: (List any pip dependencies required specifically for this module).

## 3. Frontend
- Odoo OWL Framework (for any custom web client components).
- Standard Odoo XML views.
